# 📖 W1 基础语法 —— 题目版（自测专用）

> **知识点覆盖**: 变量、数据类型、运算符、分支循环、函数、字符串、列表、字典、集合
> **建议时间**: 45-60分钟 | **总题数**: 30题
> ⚠️ **使用说明：请独立完成后再查看详解版，不要边做边看答案！**

---

## 一、选择题（每题2分，共20分）

### 🔘 第1题 — 变量与类型
以下变量命名**合法**的是？

A. `2name = "张三"`  
B. `_count = 10`  
C. `class = "一班"`  
D. `my-name = "Li"`  

---

### 🔘 第2题 — 数据类型
表达式 `type(3 // 2)` 的结果是：

A. `<class 'int'>`  
B. `<class 'float'>`  
C. `<class 'str'>`  
D. `<class 'bool'>`  

---

### 🔘 第3题 — 运算符优先级
表达式 `3 + 4 * 2 ** 2 - 1` 的结果是：

A. 18  
B. 19  
C. 20  
D. 21  

---

### 🔘 第4题 — 字符串操作
执行以下代码后，输出结果是什么？
```python
s = "Hello, Python!"
print(s[-6:-1])
```

A. `Pytho`  
B. `ython`  
C. `Python`  
D. `ytho`  

---

### 🔘 第5题 — 列表切片
```python
lst = [0, 1, 2, 3, 4, 5]
print(lst[::2])
```
输出结果是什么？

A. `[0, 2, 4]`  
B. `[1, 3, 5]`  
C. `[0, 1, 2]`  
D. `[2, 4]`  

---

### ❌ 第6题 — 字典操作
关于Python字典，以下说法**错误**的是：

A. 字典的键必须是不可变类型  
B. 可以用 `dict.get(key)` 获取值，不存在时返回 None  
C. `dict.keys()` 返回一个列表  
D. 字典在Python 3.7+中保持插入顺序  

---

### 🔘 第7题 — 集合特性
以下代码输出什么？
```python
a = {1, 2, 3}
b = {2, 3, 4}
print(a | b)
```

A. `{2, 3}`  
B. `{1, 4}`  
C. `{1, 2, 3, 4}`  
D. 报错  

---

### 🔘 第8题 — 循环与else
以下代码输出几行？
```python
for i in range(3):
    if i == 2:
        break
    print(i)
else:
    print("done")
```

A. 2行  
B. 3行  
C. 4行  
D. 1行  

---

### 🔘 第9题 — 函数参数
以下代码输出什么？
```python
def func(x, y=10, *args):
    print(x + y + sum(args))

func(1, 2, 3, 4)
```

A. 6  
B. 10  
C. 13  
D. 报错  

---

### 🔘 第10题 — 可变默认参数陷阱
经典面试题！以下代码运行结果是什么？
```python
def add_item(item, lst=[]):
    lst.append(item)
    return lst

print(add_item("a"))
print(add_item("b"))
```

A. `['a']` 和 `['b']`  
B. `['a', 'b']` 和 `['a', 'b']`  
C. `['a']` 和 `['a', 'b']`  
D. 报错  

---

## 二、填空题（每空2分，共24分）

### ✏️ 第11题 — 类型转换
将字符串 `"123.45"` 转换为浮点数应使用 `__________()` 函数。  
整数除法 `7 / 2` 在 Python 3 中结果的类型是 `__________`。

---

### ✏️ 第12题 — 格式化字符串
补全以下代码，使其输出 `姓名: 张三, 年龄: 18`：
```python
name = "张三"
age = 18
print(f"_______________")   # 使用 f-string
print("_______________".format(name, age))  # 使用 format 方法
```

---

### ✏️ 第13题 — 条件判断
补全代码，判断年份是否为闰年：
```python
year = 2024
is_leap = (year % 4 == 0) ___________ (year % 100 != 0 or year % 400 == 0)
print(is_leap)
# 输出: True
```

---

### ✏️ 第14题 — while 循环
补全代码，计算 1+2+3+...+100 的和：
```python
total = 0
i = 1
while i ___________ 100:
    total += i
    i ___________
print(total)  # 应该输出: 5050
```

---

### ✏️ 第15题 — 列表方法
```python
nums = [3, 1, 4, 1, 5, 9, 2, 6]
nums.___________()          # 升序排序
print(nums[___________])     # 找到最大值 9 的索引
print(nums.count(___________))  # 统计数字 1 出现的次数
```

---

### ✏️ 第16题 — 字典推导式
补全代码，生成平方数字典：
```python
result = {x: ___________ for x in range(1, 6)}
# 结果应为: {1: 1, 2: 4, 3: 9, 4: 16, 5: 25}
```

---

## 三、代码输出题（每题4分，共28分）

### 🖥️ 第17题
```python
a = [1, 2, 3]
b = a
b.append(4)
print(a)
print(b is a)
```
**输出:** ____________________

---

### 🖥️ 第18题
```python
msg = "  Hello World  "
print(msg.strip().upper())
print(msg.replace(" ", "-").strip())
```
**输出:** ____________________
____________________

---

### 🖥️ 第19题
```python
d = {"apple": 3, "banana": 5, "orange": 2}
for key, value in sorted(d.items(), key=lambda x: x[1]):
    print(f"{key}: {value}")
```
**输出:** ____________________

---

### 🖥️ 第20题
```python
def outer():
    x = 10
    def inner():
        nonlocal x
        x += 5
        return x
    return inner()

print(outer())
```
**输出:** ____________________

---

### 🖥️ 第21题
```python
data = [(1, "a"), (3, "c"), (2, "b")]
data.sort(key=lambda t: t[0])
for num, letter in data:
    print(letter, end="")
```
**输出:** ____________________

---

### 🖥️ 第22题 — 易错！
```python
s = "abc"
t = s.upper()
print(s, t)

lst1 = [1, 2, 3]
lst2 = lst1.copy()
lst2.append(4)
print(lst1, lst2)
```
**输出:** ____________________

---

### 🖥️ 第23题
```python
scores = {"Alice": 88, "Bob": 92, "Charlie": 78}
max_score = max(scores.values())
best_student = list(scores.keys())[list(scores.values()).index(max_score)]
print(f"最高分是 {max_score}，获得者是 {best_student}")
```
**输出:** ____________________

---

## 四、编程题（共28分）

### 💻 第24题（8分）— 温度转换器
编写函数 `celsius_to_fahrenheit(c)` 将摄氏度转为华氏度。
公式：F = C × 9/5 + 32

要求：
- 处理输入为非数字的情况（抛出 ValueError 或提示）
- 支持列表批量转换
- 示例：`celsius_to_fahrenheit([0, 100])` → `[32.0, 212.0]`

---

### 💻 第25题（8分）— 统计字符频率
编写函数 `char_count(text)` 统计字符串中每个字符出现的次数。

要求：
- 忽略大小写（'A'和'a'视为相同）
- 排除空格和标点符号
- 按出现次数从高到低返回字典
- 示例：`char_count("Hello World!")` → `{'l': 3, 'o': 2, 'h': 1, 'e': 1, 'w': 1, 'r': 1, 'd': 1}`

---

### 💻 第26题（12分）— 学生成绩管理（综合）
编写一个简单的学生成绩管理程序，包含以下功能：

1. **添加学生**：输入姓名和成绩
2. **查询成绩**：根据姓名查询成绩
3. **统计信息**：显示最高分、最低分、平均分
4. **查找不及格**：找出所有低于60分的同学
5. **按成绩排序**：按成绩从高到低排列学生

要求：
- 使用字典存储学生数据（姓名→成绩）
- 使用函数封装每个功能
- 提供简单的交互菜单（while循环 + input）

---

*📝 答完所有题目后，请对照【详解版】检查答案，标记错题并记录原因*
