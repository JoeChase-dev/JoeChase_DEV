# 📖 W2 面向对象 —— 题目版（自测专用）

> **知识点覆盖**: 类、继承、多态、魔术方法、异常处理、文件IO、模块与包
> **建议时间**: 45-60分钟 | **总题数**: 25题
> ⚠️ **使用说明：请独立完成后再查看详解版，不要边做边看答案！**

---

## 一、选择题（每题2分，共16分）

### 🔘 第1题 — 类的基本概念
以下关于Python类的说法，**正确**的是：

A. 类是对象的模板，对象是类的实例  
B. 一个类只能创建一个实例  
C. 类中的方法必须至少有一个参数（self）  
D. 以上全部正确  

---

### 🔘 第2题 — `__init__` 方法
关于 `__init__` 方法，下列描述**错误**的是：

A. 它在创建对象时自动调用  
B. 它用于初始化实例属性  
C. 它必须返回一个值  
D. 第一个参数通常是 self  

---

### 🔘 第3题 — 继承
以下代码的输出是什么？
```python
class Animal:
    def speak(self):
        return "动物叫"

class Dog(Animal):
    def speak(self):
        return "汪汪!"

d = Dog()
print(d.speak())
```

A. `动物叫`  
B. `汪汪!`  
C. 报错  
D. `None`  

---

### 🔘 第4题 — 多继承MRO
```python
class A:
    def show(self):
        print("A")

class B(A):
    def show(self):
        print("B")
        
class C(A):
    def show(self):
        print("C")

class D(B, C):
    pass

d = D()
d.show()
```
输出什么？

A. `A`  
B. `B`  
C. `C`  
D. 报错  

---

### 🔘 第5章 — 魔术方法
以下代码输出什么？
```python
class Point:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    def __str__(self):
        return f"({self.x}, {self.y})"

p = Point(3, 4)
print(p)
str(p)
```

A. `<__main__.Point object at 0x...>` 和 `<__main__.Point object at 0x...>`  
B. `(3, 4)` 和 `(3, 4)`  
C. `(3, 4)` 和 `<__main__.Point object at 0x...>`  
D. 报错

---

### 🔘 第6题 — 异常处理
以下哪种异常处理方式**最规范**？

A. `except:` 捕获所有异常，什么都不写  
B. `except Exception as e:` 捕获通用异常并打印错误信息  
C. 只用 try 不用 finally 或 else  
D. 每行代码都包一层 try-except

---

### 🔘 第7题 — 文件操作
以下关于文件操作的代码，**正确**的是：

A.
```python
f = open("test.txt", "w")
f.write("hello")
# 忘记关闭...
```

B.
```python
with open("test.txt", "r") as f:
    content = f.read()
```

C.
```python
f = open("test.txt", "r")
data = f.read()
close(f)   # 错误写法
```

D. A和B都正确

---

### 🔘 第8题 — 模块导入
在同一个目录下有 `my_module.py`，其中定义了函数 `greet()`。以下**不能**成功导入的方式是：

A. `import my_module; my_module.greet()`  
B. `from my_module import greet; greet()`  
C. `from my_module import *; greet()`  
D. `import greet from my_module`  

---

## 二、填空题（每空2分，共20分）

### ✏️ 第9题 — 类属性 vs 实例属性
```python
class Dog:
    species = "犬类"      # 类属性（所有实例共享）
    
    def __init__(self, name):
        self.name = name   # 实例属性（每个实例独立）

d1 = Dog("旺财")
d2 = Dog("大黄")

Dog.species = "修改后的物种"  # 修改类属性
print(d1.species)           # 输出: ____________
print(d2.__class__.__name__)  # 输出: ____________
```

---

### ✏️ 第10题 — 继承与super
```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age
        
class Student(Person):
    def __init__(self, name, age, student_id):
        ____________.__init__(name, age)  # 调用父类构造方法
        self.student_id = student_id

s = Student("张三", 18, "S20240001")
print(s.name)       # 输出: ____________
print(s.student_id) # 输出: ____________
```

---

### ✏️ 第11题 — 异常类型
补全代码：
```python
try:
    result = 10 / 0
except ____________:          # 除零异常
    print("除数不能为0！")
except ____________ as e:     # 其他异常
    print(f"发生其他错误: {e}")
```

---

### ✏️ 第12题 — 文件读写模式
```python
# 写入文件（不存在则创建，存在则清空重写）
with open("data.txt", "___________") as f:
    f.write("Hello")

# 追加写入（不覆盖原有内容）
with open("data.txt", "___________") as f:
    f.write(" World")

# 读取所有内容为字符串
with open("data.txt", "___________") as f:
    content = f.read()
    print(content)  # 输出: ____________
```

---

### ✏️ 第13题 — 常用魔术方法速查表
```python
class MyList:
    def __init__(self, items):
        self.items = items
    
    def __len__(self):              # len() 函数调用
        return len(self.items)
    
    def __getitem__(self, index):   # 索引访问 obj[index]
        return self.items[___________]
    
    def __contains__(self, item):   # in 运算符
        return item in self.items
    
    def __add__(self, other):       # + 运算符
        return MyList(self.items + other.items)

lst = MyList([1, 2, 3])
print(len(lst))          # 输出: ____________
print(lst[1])            # 输出: ____________
print(2 in lst)          # 输出: ____________
```

## 三、代码输出题（每题4分，共24分）

### 🖥️ 第14题 — 实例方法 vs 类方法 vs 静态方法
```python
class Demo:
    count = 0
    
    def __init__(self):
        Demo.count += 1
    
    @classmethod
    def get_count(cls):
        return cls.count
    
    @staticmethod
    def info():
        return "这是一个Demo类"

a = Demo()
b = Demo()
c = Demo()

print(Demo.get_count())
print(Demo.info())
print(a.count)
```
**输出：**
____________________
____________________
____________________

---

### 🖥️ 第15题 — 属性封装
```python
class BankAccount:
    def __init__(self, balance):
        self._balance = balance  # 单下划线：约定私有
    
    @property
    def balance(self):
        return self._balance
    
    @balance.setter
    def balance(self, value):
        if value < 0:
            raise ValueError("余额不能为负!")
        self._balance = value

acct = BankAccount(1000)
print(acct.balance)

acct.balance = 2000
print(acct._balance)

acct.balance = -500
```
**输出：**
____________________
____________________
____________________

---

### 🖥️ 第16题 — 多态
```python
class Shape:
    def area(self):
        pass

class Rectangle(Shape):
    def __init__(self, w, h):
        self.w = w
        self.h = h
    def area(self):
        return self.w * self.h

class Circle(Shape):
    def __init__(self, r):
        self.r = r
    def area(self):
        return 3.14 * self.r ** 2

shapes = [Rectangle(3, 4), Circle(5)]
for s in shapes:
    print(f"{s.__class__.__name__}: {s.area()}")
```
**输出：**

---

### 🖥️ 第17题 — 异常处理链
```python
def divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        print("⚠️ 被除数为0!")
        return None
    finally:
        print("🧹 清理工作完成...")

print(divide(10, 2))
print("---")
print(divide(10, 0))
```
**输出：**

---

### 🖥️ 第18题 — 自定义异常
```python
class AgeError(Exception):
    """年龄不合法异常"""
    def __init__(self, age, message=f"年龄必须在0-150之间"):
        self.age = age
        super().__init__(message)

def set_age(age):
    if not isinstance(age, int) or age < 0 or age > 150:
        raise AgeError(age)
    return f"年龄设置为: {age}"

try:
    print(set_age(20))
    print(set_age(-5))
except AgeError as e:
    print(f"❌ 年龄{e.age}无效: {e}")
```
**输出：**

---

### 🖥️ 第19题 — 文件逐行读取 + 上下文管理器
假设 `scores.txt` 内容如下：
```
Alice,88
Bob,92
Charlie,78
```
```python
results = []
with open("scores.txt", "r") as f:
    for line in f:
        line = line.strip()
        if line:
            name, score = line.split(",")
            results.append((name, int(score)))

total = sum(score for _, score in results)
avg = total / len(results)
print(f"共有 {len(results)} 名学生")
print(f"总分: {total}, 平均分: {avg:.1f}")
```
**输出：**

---

## 四、编程题（共40分）

### 💻 第20题（12分）— 图书管理系统（面向对象）
使用面向对象设计一个简易图书管理系统：

**要求实现的类：**
1. **Book 类**：书名、作者、ISBN、是否借出
2. **Library 类**（管理多本书）：
   - `add_book(book)` — 添加图书
   - `borrow_book(isbn)` — 借书（标记借出状态）
   - `return_book(isbn)` — 还书
   - `list_books()` — 列出所有书籍及其状态
   - `search(keyword)` — 按关键词搜索书名或作者

**要求：**
- 使用@property管理借出状态
- 借书时检查是否已被借出，还书时检查是否已在馆中
- 自定义 `BookNotFoundError`, `BookAlreadyBorrowedError` 等异常

---

### 💻 第21题（10分）— 日志记录器
编写一个日志工具类 `Logger`，支持将日志同时输出到**控制台**和**文件**：

**功能要求：**
1. 支持不同级别：`DEBUG`, `INFO`, `WARNING`, `ERROR`
2. 格式：`[时间] [级别] 消息内容`
3. 写入到 `logs/` 目录下的日期命名文件（如 `2026-04-17.log`）
4. 自动创建目录（如果不存在）
5. 提供 `log(level, message)` 方法和便捷方法 `info(msg)`, `error(msg)`

---

### 💻 第22题（10分）— 学生数据持久化
基于W1的学生成绩管理系统，增加**文件持久化**功能：

**要求：**
1. 将学生数据保存为JSON格式文件 `students.json`
2. 程序启动时自动加载已有数据
3. 每次增删改后自动保存
4. 数据损坏时给出提示并从空数据开始
5. 使用 `json` 模块的 `dump()` / `load()`

---

### 💻 第23题（8分）— 包结构设计
设计一个名为 `utils` 的工具包，包含以下模块：

```
utils/
├── __init__.py        # 导出常用函数
├── string_utils.py    # 字符串工具函数（反转/统计/格式化）
├── math_utils.py      # 数学工具（阶乘/斐波那契/质数判断）
└── file_utils.py      # 文件工具（读取/写入/备份）
```

每个模块至少实现3个实用函数。编写 `__init__.py` 使得可以用 `from utils import *` 导入所有公共函数。

---

*📝 答完所有题目后，请对照【详解版】检查答案，标记错题并记录原因*
