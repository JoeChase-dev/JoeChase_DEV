# 第2月 W3：数据结构入门 + LeetCode启动 题目版

## 第一部分：选择题（15题，每题4分，共60分）

### 1. 数组与链表区别
以下关于数组和链表的说法，正确的是？
A. 数组在内存中连续存储，链表不一定连续  
B. 链表支持O(1)时间随机访问任意元素  
C. 数组插入/删除元素的时间复杂度是O(1)  
D. 链表比数组更节省内存

### 2. 栈的特性
栈（Stack）的数据结构遵循什么原则？
A. FIFO（先进先出）  
B. LIFO（后进先出）  
C. 优先级队列  
D. 随机访问

### 3. 队列的应用场景
以下哪个场景最适合使用队列？
A. 函数调用栈  
B. 浏览器前进后退  
C. 打印任务排队  
D. 表达式求值

### 4. 哈希表冲突解决
以下哪个不是哈希表解决冲突的方法？
A. 链地址法（Chaining）  
B. 开放地址法（Open Addressing）  
C. 再哈希法（Rehashing）  
D. 二叉树法（Binary Tree）

### 5. 二叉树遍历
对于二叉树，以下哪种遍历顺序是"根-左-右"？
A. 前序遍历（Pre-order）  
B. 中序遍历（In-order）  
C. 后序遍历（Post-order）  
D. 层序遍历（Level-order）

### 6. BFS vs DFS
关于广度优先搜索（BFS）和深度优先搜索（DFS），正确的是？
A. BFS通常使用栈实现  
B. DFS通常使用队列实现  
C. BFS适合找到最短路径（无权图）  
D. DFS比BFS更节省内存

### 7. LeetCode Easy题特征
以下哪类问题通常属于LeetCode Easy难度？
A. 动态规划优化问题  
B. 图论最短路径  
C. 数组/字符串基本操作  
D. 红黑树实现

### 8. 两数之和（Two Sum）
"两数之和"问题的核心解法是？
A. 暴力枚举O(n²)  
B. 哈希表O(n)  
C. 双指针O(n log n)  
D. 以上都是可行解法

### 9. 反转链表（Reverse Linked List）
反转单向链表的时间复杂度是？
A. O(1)  
B. O(log n)  
C. O(n)  
D. O(n²)

### 10. 有效括号（Valid Parentheses）
判断括号是否有效，最适合使用哪种数据结构？
A. 数组  
B. 链表  
C. 栈  
D. 队列

### 11. 链表环检测（Linked List Cycle）
检测链表是否有环，最优解法的时间复杂度是？
A. O(n)  
B. O(n log n)  
C. O(n²)  
D. O(1)

### 12. 合并两个有序数组（Merge Sorted Array）
合并两个已排序数组，最优时间复杂度是？
A. O(m+n)  
B. O(m×n)  
C. O((m+n) log(m+n))  
D. O(min(m, n))

### 13. 最大子数组和（Maximum Subarray）
Kadane算法解决最大子数组和问题的时间复杂度是？
A. O(n)  
B. O(n log n)  
C. O(n²)  
D. O(n³)

### 14. 二叉树的最大深度（Maximum Depth of Binary Tree）
计算二叉树最大深度的最优解法是？
A. BFS层序遍历  
B. DFS递归  
C. 两种都可以，时间复杂度都是O(n)  
D. BFS比DFS快

### 15. 对称二叉树（Symmetric Tree）
判断二叉树是否对称，核心思路是？
A. 比较左右子树镜像  
B. 中序遍历结果对称  
C. 计算节点个数  
D. 检查平衡因子

---

## 第二部分：编程题（5题，每题8分，共40分）

### 1. 实现基本数据结构
手工实现以下数据结构（不要使用内置的`list`作为底层，除非必要）：

**a) 链表节点和基本操作**
```python
class ListNode:
    def __init__(self, val=0):
        self.val = val
        self.next = None

class LinkedList:
    def __init__(self):
        self.head = None
    
    def append(self, val):
        """在链表末尾添加节点"""
        pass
    
    def prepend(self, val):
        """在链表头部添加节点"""
        pass
    
    def delete(self, val):
        """删除第一个值为val的节点"""
        pass
    
    def display(self):
        """打印链表"""
        pass
```

**b) 栈（使用链表实现）**
```python
class Stack:
    def __init__(self):
        pass
    
    def push(self, val):
        """入栈"""
        pass
    
    def pop(self):
        """出栈"""
        pass
    
    def peek(self):
        """查看栈顶元素"""
        pass
    
    def is_empty(self):
        """判断栈是否为空"""
        pass
```

### 2. LeetCode 1：两数之和（Two Sum）
给定一个整数数组`nums`和一个整数目标值`target`，请你在该数组中找出**和为目标值**的那**两个**整数，并返回它们的数组下标。

**要求**：
- 你可以假设每种输入只会对应一个答案
- 不能重复利用同一个元素
- 时间复杂度O(n)

```python
def two_sum(nums, target):
    """
    示例：
    输入：nums = [2, 7, 11, 15], target = 9
    输出：[0, 1]  # 因为 nums[0] + nums[1] = 2 + 7 = 9
    """
    pass

# 测试
print(two_sum([2, 7, 11, 15], 9))  # [0, 1]
print(two_sum([3, 2, 4], 6))       # [1, 2]
print(two_sum([3, 3], 6))          # [0, 1]
```

### 3. LeetCode 20：有效的括号（Valid Parentheses）
给定一个只包括`'('`，`')'`，`'{'`，`'}'`，`'['`，`']'`的字符串`s`，判断字符串是否有效。

有效字符串需满足：
1. 左括号必须用相同类型的右括号闭合
2. 左括号必须以正确的顺序闭合

```python
def is_valid(s):
    """
    示例：
    输入：s = "()"       输出：True
    输入：s = "()[]{}"   输出：True
    输入：s = "(]"       输出：False
    输入：s = "([)]"     输出：False
    """
    pass

# 测试
test_cases = ["()", "()[]{}", "(]", "([)]", "{[]}", "", "((()))"]
for test in test_cases:
    print(f"{test}: {is_valid(test)}")
```

### 4. LeetCode 21：合并两个有序链表（Merge Two Sorted Lists）
将两个升序链表合并为一个新的**升序**链表并返回。新链表是通过拼接给定的两个链表的所有节点组成的。

```python
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

def merge_two_lists(l1, l2):
    """
    示例：
    输入：l1 = [1,2,4], l2 = [1,3,4]
    输出：[1,1,2,3,4,4]
    """
    pass

# 辅助函数：创建链表
def create_list(arr):
    if not arr:
        return None
    head = ListNode(arr[0])
    current = head
    for val in arr[1:]:
        current.next = ListNode(val)
        current = current.next
    return head

# 辅助函数：打印链表
def print_list(head):
    values = []
    while head:
        values.append(head.val)
        head = head.next
    print(values)

# 测试
l1 = create_list([1, 2, 4])
l2 = create_list([1, 3, 4])
merged = merge_two_lists(l1, l2)
print_list(merged)  # 应该输出 [1, 1, 2, 3, 4, 4]
```

### 5. LeetCode 104：二叉树的最大深度（Maximum Depth of Binary Tree）
给定一个二叉树，找出其最大深度。二叉树的深度为根节点到最远叶子节点的最长路径上的节点数。

```python
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

def max_depth(root):
    """
    示例：
    输入：root = [3,9,20,null,null,15,7]
        3
       / \
      9  20
         / \
        15  7
    输出：3
    """
    pass

# 辅助函数：创建测试二叉树
def create_test_tree():
    # 创建上述示例树
    root = TreeNode(3)
    root.left = TreeNode(9)
    root.right = TreeNode(20)
    root.right.left = TreeNode(15)
    root.right.right = TreeNode(7)
    return root

# 测试
tree = create_test_tree()
print(max_depth(tree))  # 应该输出 3
print(max_depth(None))  # 应该输出 0
```

---

## 第三部分：数据结构概念题（5题，每题4分，共20分）

### 1. 时间复杂度分析
分析以下操作的时间复杂度：
- 数组随机访问（已知索引）
- 链表查找元素（未知位置）
- 哈希表查找元素（平均情况）
- 栈的入栈操作
- 队列的出队操作

### 2. 空间复杂度比较
比较以下数据结构的空间开销（不考虑存储数据本身）：
- 数组 vs 链表
- 哈希表 vs 二叉树
- 栈（数组实现）vs 栈（链表实现）

### 3. 实际应用场景匹配
将以下场景与最适合的数据结构匹配：
1. 浏览器前进后退功能
2. 操作系统进程调度
3. 电话通讯录快速查找
4. 表达式求值（如计算器）
5. 社交网络好友关系

选项：A. 栈  B. 队列  C. 哈希表  D. 图  E. 数组

### 4. 算法策略选择
对于以下问题，选择最优算法策略：
1. 查找数组中的最大值
2. 判断字符串是否回文
3. 查找排序数组中的元素
4. 计算斐波那契数列
5. 排序整数数组

选项：A. 线性扫描  B. 双指针  C. 二分查找  D. 动态规划  E. 快速排序

### 5. LeetCode刷题策略
制定一个合理的LeetCode刷题计划，包括：
- 每周刷题数量
- 题目难度分布
- 时间分配（学习vs刷题）
- 如何记录和复习

---

**总分：120分**（选择题60 + 编程题40 + 概念题20）

**答题建议**：
1. 选择题考察对数据结构核心概念的理解
2. 编程题需要实际编码，注意边界条件和效率
3. 数据结构实现题注意指针/引用操作
4. LeetCode题要理解最优解法，不仅仅是AC
5. 概念题需要清晰表达思路和理由