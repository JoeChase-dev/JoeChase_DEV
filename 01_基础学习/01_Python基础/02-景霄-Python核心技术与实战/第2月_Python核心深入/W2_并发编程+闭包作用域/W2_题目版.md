# 第2月 W2：并发编程 + 闭包作用域 题目版

## 第一部分：选择题（15题，每题4分，共60分）

### 1. GIL全局解释器锁
关于Python的GIL（Global Interpreter Lock），以下说法正确的是？
A. GIL确保多线程程序绝对线程安全  
B. GIL只影响CPython，不影响Jython或IronPython  
C. GIL允许同一时刻多个线程执行Python字节码  
D. 使用多进程可以完全避免GIL的影响

### 2. threading vs multiprocessing
在Python中，处理CPU密集型任务时，应该选择？
A. `threading`模块，因为线程轻量级  
B. `multiprocessing`模块，因为可以绕过GIL  
C. `asyncio`模块，因为协程效率高  
D. 单线程，因为Python不适合CPU密集型任务

### 3. 线程安全操作
以下哪个操作在Python多线程环境中是线程安全的？
A. `list.append(item)`  
B. `counter += 1`（counter是整数）  
C. `dict[key] = value`  
D. 以上都不是完全线程安全的

### 4. asyncio基础
`asyncio`的主要优势是什么？
A. 提高CPU密集型任务性能  
B. 高效处理I/O密集型任务，避免线程切换开销  
C. 完全替代多线程和多进程  
D. 自动并行执行多个CPU任务

### 5. 闭包变量捕获
```python
def outer():
    x = 10
    def inner():
        print(x)
    return inner

func = outer()
func()  # 输出什么？
```
A. 报错，x未定义  
B. 输出`10`  
C. 输出`None`  
D. 输出`<function inner>`

### 6. LEGB规则
在Python作用域查找中，LEGB代表什么顺序？
A. Local → External → Global → Built-in  
B. Local → Enclosing → Global → Built-in  
C. Lexical → Enclosing → Global → Built-in  
D. Local → Extended → Global → Built-in

### 7. nonlocal关键字
```python
def outer():
    x = 0
    def inner():
        nonlocal x
        x += 1
        return x
    return inner

counter = outer()
print(counter())  # 输出？
print(counter())  # 输出？
```
A. 1, 2  
B. 1, 1  
C. 报错  
D. 0, 1

### 8. 线程创建方式
以下哪种方式可以创建并启动线程？
A. `thread = threading.Thread(target=func); thread.run()`  
B. `thread = threading.Thread(target=func); thread.start()`  
C. `thread = threading.Thread(target=func()); thread.start()`  
D. `thread = threading.Thread(func); thread.start()`

### 9. 进程间通信
`multiprocessing`模块中，以下哪种方式不能用于进程间通信？
A. `Queue`  
B. `Pipe`  
C. `shared memory`  
D. `global variable`

### 10. asyncio事件循环
关于`asyncio`事件循环，正确的是？
A. 每个线程可以有多个事件循环  
B. `asyncio.run()`会自动创建新的事件循环  
C. 事件循环只能运行在main线程  
D. 事件循环可以同时运行多个CPU密集型任务

### 11. 闭包与装饰器关系
装饰器本质上是什么？
A. 闭包的特殊形式  
B. 类的实例  
C. 生成器函数  
D. 元类

### 12. 线程池应用
以下代码的问题是什么？
```python
import concurrent.futures

def process_item(item):
    return item * 2

items = [1, 2, 3, 4, 5]
with concurrent.futures.ThreadPoolExecutor() as executor:
    results = executor.map(process_item, items)
    print(list(results))
```
A. 应该使用`ProcessPoolExecutor`而不是`ThreadPoolExecutor`  
B. `map`方法会阻塞直到所有任务完成  
C. 没有关闭executor  
D. 代码没有问题

### 13. 协程async/await
```python
import asyncio

async def say_hello():
    print("Hello")
    await asyncio.sleep(1)
    print("World")

asyncio.run(say_hello())
```
输出顺序是？
A. Hello → (等待1秒) → World  
B. Hello和World同时打印  
C. 只打印Hello  
D. 报错

### 14. 作用域链
```python
x = "global"

def outer():
    x = "outer"
    
    def inner():
        x = "inner"
        print(x)
    
    inner()
    print(x)

outer()
print(x)
```
输出结果是？
A. inner → outer → global  
B. inner → inner → inner  
C. global → global → global  
D. 报错，变量重复定义

### 15. 死锁避免
以下哪个是避免死锁的有效策略？
A. 永远不适用锁  
B. 按固定顺序获取锁  
C. 使用更细粒度的锁  
D. 使用`threading.Lock`而不是`threading.RLock`

---

## 第二部分：编程题（5题，每题8分，共40分）

### 1. 多线程爬虫
编写一个多线程爬虫，同时下载多个网页内容。要求：
- 使用`concurrent.futures.ThreadPoolExecutor`
- 控制并发线程数（如5个）
- 处理异常，某个网页下载失败不影响其他任务
- 统计总下载时间和成功数量

```python
urls = [
    "https://httpbin.org/delay/1",
    "https://httpbin.org/delay/2", 
    "https://httpbin.org/delay/3",
    "https://httpbin.org/status/404",  # 会失败
    "https://httpbin.org/delay/1"
]

# 预期输出：
# Successfully downloaded: 4 pages
# Failed: 1 pages
# Total time: ~3 seconds (因为并发)
```

### 2. 进程池计算
使用`multiprocessing.Pool`计算斐波那契数列前n项（使用递归实现，模拟CPU密集型任务）。比较单进程和多进程的性能差异。

要求：
- 计算`fib(35)`多次（如10次）
- 使用`time.perf_counter()`测量时间
- 注意Windows下`multiprocessing`的特殊要求（`if __name__ == "__main__"`）

```python
def fib(n):
    if n <= 1:
        return n
    return fib(n-1) + fib(n-2)

# 单进程版本
# 多进程版本（4个进程）
# 输出比较结果
```

### 3. asyncio并发请求
使用`asyncio`和`aiohttp`（或`httpx`）并发发起多个HTTP请求。要求：
- 实现异步函数`fetch_url(url)`下载网页内容
- 使用`asyncio.gather()`并发执行多个任务
- 添加超时处理，单个请求超时不影响其他

```python
import asyncio
# 使用aiohttp或httpx

async def fetch_all(urls):
    # 实现并发下载
    pass

# 测试
urls = ["https://httpbin.org/delay/1"] * 10
# 10个请求应该大约1秒完成，而不是10秒
```

### 4. 闭包实现计数器工厂
创建一个函数`create_counter(initial=0, step=1)`，返回一个计数器函数。每次调用计数器函数，它返回递增的值。使用闭包实现。

要求：
```python
counter1 = create_counter(initial=0, step=1)
print(counter1())  # 0
print(counter1())  # 1
print(counter1())  # 2

counter2 = create_counter(initial=10, step=5)
print(counter2())  # 10
print(counter2())  # 15
print(counter2())  # 20

# 两个计数器独立工作
```

### 5. 线程安全的数据结构
实现一个线程安全的`Counter`类，支持多线程环境下的`increment()`、`decrement()`和`get_value()`操作。要求：
- 使用`threading.Lock`确保线程安全
- 支持传入初始值
- 添加`reset()`方法重置计数器

```python
import threading

class ThreadSafeCounter:
    def __init__(self, initial=0):
        # 实现
        pass
    
    def increment(self, n=1):
        # 实现
        pass
    
    def decrement(self, n=1):
        # 实现
        pass
    
    def get_value(self):
        # 实现
        pass
    
    def reset(self):
        # 实现
        pass

# 测试：多个线程同时操作计数器，最终结果应该正确
```

---

**总分：100分**

**答题建议**：
1. 选择题考察对并发模型、GIL、作用域规则的深入理解
2. 编程题需要实际编码，注意不同并发方式的适用场景
3. 多线程/多进程注意资源管理和异常处理
4. asyncio注意异步函数定义和事件循环使用
5. 闭包实现注意变量捕获和作用域