# 📖 W2 并发编程与闭包 —— 题目版（自测专用）

> **知识点覆盖**: GIL/threading/multiprocessing/asyncio基础 + 闭包/作用域LEGB规则  
> **建议时间**: 50-70分钟 | **总题数**: 23题  
> ⚠️ **使用说明：请独立完成后再查看详解版，不要边做边看答案！**

---

## 一、选择题（每题2分，共20分）

### 🔘 第1题 — GIL概念 ⭐
Python的GIL（全局解释器锁）主要影响的是？

A. 单线程程序的执行速度  
B. 多线程程序的并行执行（CPU密集型）  
C. 多进程间的数据共享  
D. asyncio异步任务的执行  

---

### 🔘 第2题 — GIL与多线程 ⭐⭐
关于Python多线程，以下说法正确的是？

A. Python多线程可以充分利用多核CPU  
B. Python多线程适合IO密集型任务  
C. Python多线程适合CPU密集型任务  
D. GIL可以用`threading`模块关闭  

---

### 🔘 第3题 — threading模块 ⭐
以下代码同时启动5个线程，预期输出是？（假设线程执行顺序不确定）

```python
import threading

def worker():
    print("working", end=" ")

threads = []
for i in range(5):
    t = threading.Thread(target=worker)
    threads.append(t)
    t.start()

for t in threads:
    t.join()
```

A. `working working working working working`（一定顺序）  
B. `working` 出现5次，但顺序不确定  
C. 只输出1次 `working`  
D. 报错  

---

### 🔘 第4题 — multiprocessing ⭐⭐
`multiprocessing` 模块相比 `threading` 的主要优势是？

A. 创建线程的代价更小  
B. 可以绕过GIL，实现真正的多核并行  
C. 数据共享更方便  
D. 代码更简洁  

---

### 🔘 第5题 — asyncio基础 ⭐⭐
以下代码中，`async def` 定义的函数叫什么？

```python
async def fetch_data():
    await some_io_operation()
```

A. 生成器  
B. 协程（coroutine）  
C. 线程函数  
D. 普通函数  

---

### 🔘 第6题 — await/async ⭐⭐
以下代码的输出顺序是什么？

```python
import asyncio

async def task1():
    print("A")
    await asyncio.sleep(1)
    print("B")

async def task2():
    print("C")
    await asyncio.sleep(0.5)
    print("D")

asyncio.run(asyncio.gather(task1(), task2()))
```

A. `A B C D`  
B. `A C D B`  
C. `A C B D`  
D. 顺序不确定  

---

### 🔘 第7题 — 闭包概念 ⭐
以下代码中，`inner` 函数形成了闭包，它捕获了哪个变量？

```python
def outer():
    x = 10
    def inner():
        print(x)
    return inner

f = outer()
f()
```

A. 没有捕获任何变量  
B. 捕获了 `x`  
C. 捕获了 `outer`  
D. 捕获了 `f`  

---

### 🔘 第8题 — LEGB规则 ⭐⭐
Python查找变量的顺序是什么？

A. Local → Enclosing → Global → Built-in  
B. Global → Local → Enclosing → Built-in  
C. Built-in → Global → Enclosing → Local  
D. Enclosing → Local → Global → Built-in  

---

### 🔘 第9题 — nonlocal关键字 ⭐⭐
以下代码输出什么？

```python
def outer():
    x = 0
    def inner():
        nonlocal x
        x += 1
        return x
    return inner

counter = outer()
print(counter())
print(counter())
```

A. `0` 和 `0`  
B. `1` 和 `1`  
C. `1` 和 `2`  
D. 报错  

---

### 🔘 第10题 — 闭包经典陷阱 ⭐⭐⭐
以下代码输出什么？

```python
funcs = []
for i in range(3):
    def f():
        return i
    funcs.append(f)

for f in funcs:
    print(f(), end=" ")
```

A. `0 1 2`  
B. `2 2 2`  
C. `0 0 0`  
D. 报错  

---

## 二、代码输出题（每题4分，共28分）

### 🖥️ 第11题 — GIL演示 ⭐⭐

```python
import threading
import time

def cpu_bound():
    x = 0
    for _ in range(10**7):
        x += 1
    return x

t1 = threading.Thread(target=cpu_bound)
t2 = threading.Thread(target=cpu_bound)

start = time.time()
t1.start(); t2.start()
t1.join(); t2.join()
print(f"耗时: {time.time() - start:.2f}秒")
```

如果单线程执行 `cpu_bound()` 需要2秒，双线程大约耗时？  
**答：** ____________________

---

### 🖥️ 第12题 — multiprocessing加速 ⭐⭐

```python
import multiprocessing as mp

def square(x):
    return x * x

if __name__ == "__main__":
    with mp.Pool(processes=4) as pool:
        results = pool.map(square, range(10))
        print(results)
```

`if __name__ == "__main__":` 这行在Windows上是？  
**答：** ____________________

---

### 🖥️ 第13题 — asyncio并发 ⭐⭐

```python
import asyncio

async def say_hello(delay, name):
    await asyncio.sleep(delay)
    print(f"Hello, {name}!", end=" ")

async def main():
    await asyncio.gather(
        say_hello(1, "Alice"),
        say_hello(2, "Bob"),
        say_hello(3, "Charlie")
    )

asyncio.run(main())
```

总耗时大约多少秒？  
**答：** ____________________

---

### 🖥️ 第14题 — 闭包捕获 ⭐⭐

```python
def make_multiplier(n):
    def multiplier(x):
        return x * n
    return multiplier

double = make_multiplier(2)
triple = make_multiplier(3)

print(double(5))
print(triple(5))
```

**输出:** ____________________

---

### 🖥️ 第15题 — LEGB查找 ⭐⭐

```python
x = "global"

def outer():
    x = "enclosing"
    def inner():
        x = "local"
        print(x)
    inner()

outer()
print(x)
```

**输出:** ____________________  
____________________

---

### 🖥️ 第16题 — nonlocal vs global ⭐⭐⭐

```python
count = 0

def counter_factory():
    count = 0
    def counter():
        nonlocal count
        count += 1
        return count
    return counter

c = counter_factory()
print(c())
print(c())
print(count)
```

**输出:** ____________________

---

### 🖥️ 第17题 — 闭包陷阱修复 ⭐⭐⭐

以下代码输出 `2 2 2`，如何修复使其输出 `0 1 2`？

```python
funcs = []
for i in range(3):
    def f():
        return i
    funcs.append(f)
```

请在下方写出修复后的代码：  
**答：** ____________________

---

## 三、手写代码题（共32分）

### ✍️ 第18题（10分）— 多线程爬虫模拟 ⭐⭐
使用 `threading` 模块模拟多线程爬虫：
- 定义 `crawl(url)` 函数，模拟爬取（用 `time.sleep(1)` 模拟IO）
- 同时启动3个线程爬取不同URL
- 主线程等待所有线程完成
- 打印总耗时（应该约1秒，而不是3秒）

---

### ✍️ 第19题（10分）— asyncio异步爬虫 ⭐⭐⭐
使用 `asyncio` 实现异步爬虫：
- 定义 `async def fetch(url)` 协程，用 `await asyncio.sleep()` 模拟IO
- 同时发起3个异步请求
- 使用 `asyncio.gather()` 收集结果
- 打印总耗时

---

### ✍️ 第20题（12分）— 闭包实现计数器 ⭐⭐
使用闭包实现一个计数器工厂函数：
- `make_counter(initial=0)` 返回一个计数器函数
- 每次调用计数器函数，返回下一个值
- 支持多个独立计数器

---

## 四、综合应用题（共20分）

### 🎯 第21题（10分）— GIL理解与应对 ⭐⭐⭐
简答题：
1. 什么是GIL？为什么Python（CPython）要有GIL？
2. GIL对多线程程序的影响是什么？
3. 如何应对GIL的限制？（至少说出3种方法）

---

### 🎯 第22题（10分）— 并发模型选择 ⭐⭐
场景题：以下场景应该选择哪种并发模型（threading/multiprocessing/asyncio）？说明理由。
1. 爬取100个网页（网络IO密集）
2. 计算100万个素数的个数（CPU密集）
3. 高并发Web服务器（大量网络连接）
4. 批量处理图片（CPU密集+IO密集混合）

---

## 🏆 LeetCode 本周预备题

### 🏆 第23题 — LC #1114 按序打印（并发基础）
使用 `threading` 的同步原语（Lock/Event/Semaphore）实现三个函数按顺序执行。

---

*📝 答完所有题目后，请对照【详解版】检查答案，标记错题并记录原因*  
*💡 重点：GIL概念、asyncio协程、闭包捕获、LEGB规则*
